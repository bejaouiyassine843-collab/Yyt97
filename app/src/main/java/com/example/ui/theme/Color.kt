package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Clean Minimalism Color Palette
val DeepBlack = Color(0xFF0A0A0A)       // Ultra-dark canvas background
val StudioCard = Color(0xFF1A1A1A)      // Soft elevated dark surface for cards and inputs
val StudioSurface = Color(0xFF0F0F0F)   // Intermediate surface
val YoutubeRed = Color(0xFFDC2626)      // Vibrant minimalistic red accent (Tailwind Red 600)
val AccentGrey = Color(0xFF222222)      // Border color / active indicator
val BorderWhiteTranslucent = Color(0x0DFFFFFF) // Tailwind white/5
val TextPrimary = Color(0xFFFFFFFF)     // Sharp clean white text
val TextSecondary = Color(0xFFA1A1AA)   // Zinc-400 premium secondary text
val MutedGrey = Color(0xFF71717A)       // Zinc-500 muted text
val PureWhite = Color(0xFFFFFFFF)

